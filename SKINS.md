# 皮肤画廊 · Skin Gallery

<p align="center"><a href="README.md">← 回到 README</a> · <a href="README_EN.md">English README</a></p>

别人的桌宠只能长成作者画的样子。这一只可以长成你的猫。

这一页收社区做出来的皮肤。**投稿不需要交素材**——只要一张预览图和一行说明，
你的十八张图留在你自己机器上。

---

## 怎么做一套

把一张图交给你的 agent，说「做一套桌宠皮肤」。插件里带的 `dsh-pet-skin` skill
会把它扩写成一套皮肤需要的十八个姿势（六个状态 × 三帧），
用**你自己的画图工具、你自己的额度**生成，我们不往任何地方发送东西。

它中途会停两次：第一次在基准姿势之后，让你在多花十七张之前先看看要不要；
第二次在第二帧之后，确认角色重画之后还是同一个。

细节见 README 的「皮肤」一节。

## 怎么投稿

**一、生成预览图**（一条命令，不用 clone 这个仓库）

启动器在插件包里，不在 PATH 上。**需要 0.3.0 或更新**——旧版本没有这个参数，升级用 `dsh plugin --profile web add deepseek-desk-pet@latest`。

```bash
# 作为 DSH 插件装的（web profile；换成你实际用的 profile 名）
~/.dsh/profiles/web/node_modules/deepseek-desk-pet/bin/dsh-desk-pet \
  --skin-sheet <你的皮肤id>

# clone 了这个仓库的话
./bin/dsh-desk-pet --skin-sheet mycat --out ~/Desktop/mycat.png
```

它会把这套皮肤的六个状态拼成一张图，默认写到当前目录的 `skin-<id>.png`。
跑不出来先用 `--inventory` 核对 id。

> 让你的 agent 帮你跑这条更省事：它知道插件装在哪。

**二、提一个 PR**，只做两件事：

1. 把预览图放进 `docs/gallery/<你的皮肤id>.png`
2. 在下面的表格末尾加一行

```markdown
| <皮肤名> | ![](docs/gallery/<id>.png) | [@你的GitHub](https://github.com/你) | <一句话，可留空> |
```

就这样。不要提交帧素材——十八张 PNG 会让这个仓库很快胖起来，
而且那是从你的照片生成的东西，该由你决定它去哪里。

## 投稿须知

- **只投你有权分发的图。** 从别人的照片、有版权的角色、真人肖像生成的皮肤不收。
  自己的宠物、自己画的、自己拥有的形象——都可以。
- **id 规则**：小写字母开头，只含小写字母、数字、`-`、`_`，最长 32 位（安装器强制）。
- 预览图就用 `--skin-sheet` 出的那张，别自己拼——统一底板才能一眼横向比较。
- 一个 PR 一套皮肤。
- 收录不代表背书；这一页是社区作品陈列，不是质量认证。

---

## 画廊

内置的五套在最前面，社区投稿接在后面。

| 皮肤 | 预览 | 作者 | 说明 |
|---|---|---|---|
| 深索鲸 | ![deepseek](docs/media/skin-deepseek.png) | 内置 | 默认皮肤 |
| 蓝鲸 | ![bluewhale](docs/media/skin-bluewhale.png) | 内置 | |
| 线核 | ![threadcore](docs/media/skin-threadcore.png) | 内置 | |
| 鹦鹉螺 | ![nautilus](docs/media/skin-nautilus.png) | 内置 | |
| 水母 | ![jellyfish](docs/media/skin-jellyfish.png) | 内置 | |

<!-- 社区投稿从这一行下面开始追加 -->

---

<a name="english"></a>

## English

Other desk pets can only look the way their author drew them. This one can look
like your cat.

This page collects skins the community made. **You do not submit artwork** — a
preview image and one line is the whole contribution; your eighteen frames stay
on your own machine.

### Make one

Hand an image to your agent and ask for a desk pet skin. The bundled
`dsh-pet-skin` skill expands it into the eighteen poses a skin needs (six states
× three frames), generated by **your own image tool on your own credentials**.
Nothing is sent anywhere by us.

### Submit

**1. Generate the preview** — one command, no need to clone this repo:

The launcher lives inside the installed package, not on your PATH, and this flag needs **0.3.0 or newer** — upgrade with `dsh plugin --profile web add deepseek-desk-pet@latest`.

```bash
~/.dsh/profiles/web/node_modules/deepseek-desk-pet/bin/dsh-desk-pet \
  --skin-sheet <your-skin-id>
```

It writes `skin-<id>.png` in the current directory. Asking your agent to run it
is easier — it knows where the package landed.

**2. Open a PR** that does exactly two things: put the image at
`docs/gallery/<id>.png`, and append one row to the table above.

### Rules

- **Only submit images you have the right to distribute.** Skins generated from
  someone else's photo, a copyrighted character, or a real person's likeness are
  not accepted. Your own pet, your own drawing, your own character — all fine.
- **Skin ids** start with a lowercase letter or digit and contain only lowercase
  letters, digits, `-` and `_`, up to 32 characters. The installer enforces this.
- Use the image `--skin-sheet` produces rather than composing your own — a shared
  plate is what makes the gallery comparable at a glance.
- One skin per pull request.
- Listing is not an endorsement. This is a showcase, not a quality bar.
